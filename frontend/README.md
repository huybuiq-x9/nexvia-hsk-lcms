# LCMS Frontend

React + Vite frontend for the LCMS application.

## Setup

```bash
npm install
cp .env.example .env
npm run dev
```

The login screen calls the backend endpoint configured by `VITE_API_BASE_URL`.
The default value is `http://localhost:8000/api/v1`.
